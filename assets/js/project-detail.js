const projectRoot = document.querySelector("[data-project-root]");
const safe = (value = "") => String(value).replaceAll("&", "&amp;").replaceAll("<", "&lt;").replaceAll(">", "&gt;").replaceAll('"', "&quot;").replaceAll("'", "&#039;");
const outLink = (link) => `<a href="${safe(link.url)}" target="_blank" rel="noreferrer">${safe(link.label)} ↗</a>`;

function renderProject(project) {
  document.title = `${project.title} | Md Ether Deowan`;
  const primary = project.paper || project.code;
  const media = project.video
    ? `<div class="video-shell"><iframe src="${safe(project.video)}" title="${safe(project.title)} demonstration" loading="lazy" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" allowfullscreen></iframe></div>`
    : `<figure class="case-figure case-project-hero"><img src="${safe(project.heroImage)}" alt="${safe(project.title)} project environment"></figure>`;
  const metrics = project.metrics.length ? `<div class="metrics-grid">${project.metrics.map((metric) => `<article><strong>${safe(metric.value)}</strong><span>${safe(metric.label)}</span><p>${safe(metric.note)}</p></article>`).join("")}</div>` : `<p class="evidence-note">The available project material is qualitative; no numerical benchmark is claimed here.</p>`;
  projectRoot.innerHTML = `<article class="case-study"><header class="case-hero"><div class="container"><a class="case-back" href="projects.html">← All projects</a><div class="case-hero-grid"><div><p class="eyebrow">${safe(project.kicker)} · ${safe(project.year)}</p><h1>${safe(project.title)}</h1><p class="lead">${safe(project.summary)}</p><div class="button-row">${primary ? `<a class="button" href="${safe(primary)}" target="_blank" rel="noreferrer">${project.paper ? "Read paper" : "View source code"} ↗</a>` : ""}${project.video ? `<a class="button button-secondary" href="${safe(project.video.replace("www.youtube-nocookie.com/embed/", "www.youtube.com/watch?v="))}" target="_blank" rel="noreferrer">Watch demonstration ↗</a>` : ""}</div></div><dl class="case-facts">${project.facts.map((fact) => `<div><dt>${safe(fact.label)}</dt><dd>${safe(fact.value)}</dd></div>`).join("")}</dl></div></div></header><section class="case-media-band"><div class="container">${media}</div></section><nav class="case-nav" aria-label="On this page"><div class="container"><span>On this page</span><a href="#overview">Overview</a><a href="#pipeline">Pipeline</a><a href="#evidence">Evidence</a><a href="#limitations">Limitations</a><a href="#resources">Resources</a></div></nav><section class="section" id="overview"><div class="container case-copy-grid"><div><p class="eyebrow">Technical overview</p><h2>Research contribution and system design</h2></div><div class="case-prose">${project.overview.map((paragraph) => `<p>${safe(paragraph)}</p>`).join("")}</div></div></section><section class="section section-muted" id="pipeline"><div class="container"><p class="eyebrow">System pipeline</p><h2>How the project works</h2><div class="case-stage-grid">${project.stages.map((stage, index) => `<article><span>0${index + 1}</span><h3>${safe(stage.title)}</h3><p>${safe(stage.text)}</p></article>`).join("")}</div></div></section><section class="section" id="evidence"><div class="container"><div class="case-copy-grid"><div><p class="eyebrow">Evidence</p><h2>Reported technical results</h2></div><div class="case-prose"><p>Values below are taken from the supplied paper or project report and are presented with their original evaluation scope.</p></div></div>${metrics}</div></section><section class="section section-muted" id="limitations"><div class="container case-copy-grid"><div><p class="eyebrow">Critical assessment</p><h2>Limitations and next validation steps</h2></div><div class="case-prose"><ul>${project.limitations.map((item) => `<li>${safe(item)}</li>`).join("")}</ul></div></div></section><section class="section case-resources" id="resources"><div class="container"><p class="eyebrow">Resources</p><h2>Paper, code and demonstrations</h2><div class="resource-grid">${project.links.map((link) => `<a href="${safe(link.url)}" target="_blank" rel="noreferrer"><strong>${safe(link.label)}</strong><span>Open resource →</span></a>`).join("")}</div></div></section></article>`;
}

const projectId = new URLSearchParams(window.location.search).get("id");
fetch("assets/data/project-details.json")
  .then((response) => { if (!response.ok) throw new Error(`Could not load project data (${response.status})`); return response.json(); })
  .then((projects) => { if (!projects[projectId]) throw new Error("Project not found"); renderProject(projects[projectId]); })
  .catch((error) => { projectRoot.innerHTML = `<section class="section"><div class="container content-width"><p class="eyebrow">Projects</p><h1>Project unavailable</h1><p>${safe(error.message)}.</p><a href="projects.html">Return to all projects</a></div></section>`; });
